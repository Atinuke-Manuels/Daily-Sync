import 'package:flutter/material.dart';

class DailyStandupScreen extends StatelessWidget {
  const DailyStandupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Daily StandUp')),
    );
  }
}
